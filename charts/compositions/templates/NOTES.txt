Thank you for installing {{ .Chart.Name }} version {{ .Chart.Version }}.

COMPATIBILITY:
-------------
This chart is compatible with:
- Kubernetes versions: 1.19 - 1.27
- Helm: v3.5+
- Crossplane: v1.x

INSTALLED COMPOSITIONS:
----------------------
The following Composite Resource Definition (XRD) and Composition have been installed:

- CognitoAuthorizedAPI XRD and Composition

USAGE:
------
You can now create instances of this composite resource:

CognitoAuthorizedAPI example:
```yaml
apiVersion: serverlesspdfchat.shortrib.io/v1alpha1
kind: CognitoAuthorizedAPI
metadata:
  name: serverless-pdf-chat
  labels:
    app.kubernetes.io/instance: serverless-pdf-chat
    app.kubernetes.io/name: serverless-pdf-chat
spec:
  region: {{ .Values.aws.region }}
  providerConfigRef:
    name: {{ .Values.aws.providerConfigName }}
  # Optional namespace for the ConfigMap (defaults to the resource's namespace)
  configMapNamespace: pdf-chat
  # Optional prefix/suffix for resource names
  namePrefix: ""
  nameSuffix: ""
  tags:
    Application: pdf-chat
    Environment: dev
```

VERIFICATION:
------------
To verify the installation, run:
  kubectl get xrd,composition

To check the status of your composite resources:
  kubectl get cognitoauthorizedapi

